// vector 简单介绍：
// vector是表示可以改变大小的数组的序列容器。
// 就像数组一样，向量使用连续的存储位置作为元素，
// 这意味着它们的元素也可以使用常量指向其元素的“下标”来访问，
// 并且与数组一样有效。但与数组不同，它们的大小可以动态变化，其存储由容器自动处理。
// 在使用它时, 需要包含头文件 vector： #include<vector>
#include<iostream>

int main(){
  std::cout<<1;
  return 0;
}